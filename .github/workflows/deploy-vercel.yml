# Деплой Next.js на Vercel (полная версия сайта с API и админкой).
# Настройка: https://vercel.com/docs/deployments/git/vercel-for-github
#
# 1. Импортируйте репозиторий на vercel.com (рекомендуется — без секретов в GitHub).
# 2. Либо добавьте Secrets в GitHub: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID
#    и раскомментируйте job ниже.

name: Deploy Next.js to Vercel

on:
  workflow_dispatch:

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: npm
#       - run: npm ci
#       - uses: amondnet/vercel-action@v25
#         with:
#           vercel-token: ${{ secrets.VERCEL_TOKEN }}
#           vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
#           vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
#           vercel-args: "--prod"
